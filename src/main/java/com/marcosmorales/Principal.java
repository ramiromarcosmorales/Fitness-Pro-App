package com.marcosmorales;
import com.marcosmorales.modelo.*;

import java.time.LocalDate;

public class Principal extends Socio {
    public static void main(String[] args) {
        /*
        Gimnasio gym = new Gimnasio();

        Empleado empleado1 = Empleado.crearEmpleado("Felipe", "Marcos","felipe.marcos@live.com", "1134312534", Sector.INSTRUCTOR);
        gym.contratarEmpleado(empleado1);

        Socio socio1 = Socio.crearSocio("Bautista", "Ricciardo", "bau.ricciardo@hotmail.com", "502412545");
        gym.registrarSocio(socio1);

        Maquina maquina1 = Maquina.crearMaquina(TipoMaquina.BANCO_PRESS, LocalDate.now());
        gym.registrarMaquina(maquina1);

        // Prueba de reserva con un horariio válido
        boolean result = gym.reservarClase(socio1, LocalDate.now().plusDays(100), HorarioClase.H11.getHorario());
        System.out.println("Reserva: " + (result ? "Exitosa" : "Fallida"));

        // Prueba de reservar nuevamente con el mismo horario
        try {
            gym.reservarClase(socio1, LocalDate.now().plusDays(100), HorarioClase.H11.getHorario());
        } catch (IllegalStateException e) {
            System.out.println(e.getMessage().toString());
        }
         */
    }
}
